# CORE dependencies — this is genuinely all the detector needs.
#
# Deliberately RANGES, not exact pins: an offline PyPI mirror will carry
# whatever version it carries, and pinning to a version your mirror lacks is
# the difference between a working install and a dead one. The code uses only
# long-stable numpy/Pillow API and is exercised against numpy 1.26 through
# 2.4.
numpy>=1.24,<3
pillow>=9.5

# --- deliberately NOT here -------------------------------------------------
# opencv-python-headless  optional: only accelerates connected components,
#                         morphology and blur. trojanlib.imagelib has a pure
#                         numpy fallback for every one of them.
# mcp                     optional: only for the MCP server and
#                         `screen.py remote`.
# torch / ultralytics     optional: only for the YOLO detection backend.
# scikit-image, pyyaml    were never imported by this package at all.
#
# Install the optional set with docker/requirements-optional.txt, or the
# WITH_CV2 / WITH_MCP / WITH_TORCH build args.
